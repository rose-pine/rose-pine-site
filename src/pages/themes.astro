---
import Layout from "$layouts/base.astro";
import PageIntro from "$components/page-intro.svelte";
import { themes, authors } from "$data/themes";
---

<Layout
	title="Themes"
	description="Hand-crafted collection of personalisation."
>
	<PageIntro
		heading="Themes"
		subheading="Hand-crafted collection of personalisation."
		tagline={`${themes.length} ports & ${authors.size} authors`}
	/>
</Layout>
